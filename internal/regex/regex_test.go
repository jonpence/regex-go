/* Regex tests.
 */

package regex

/*
import (
	"testing"
)

func TestInitRegex(t *testing.T) {
	testRegex := initRegex(nil)

	if &testRegex == nil {
		t.Errorf("expected non-nil value for &testRegex")
	}
}

func TestNewRegex(t *testing.T) {
	testParser := initParser()
	testRegex, ok := newRegex("A|B",&testParser)

	if !ok {
		t.Errorf("expected testRegex to successfully compile")
	}

	if &testRegex == nil {
		t.Errorf("expected non-nil value for &testRegex")
	}

	if testRegex.expression != "A|B" {
		t.Errorf("expected testRegex.expression to equal the empty string")
	}
}

func TestValidateRegex(t *testing.T) {
	testParser := initParser()
	testRegex, _ := newRegex("A|B", &testParser)

	if !testRegex.validate("A") {
		t.Errorf("expected A to be valid")
	}

	if !testRegex.validate("B") {
		t.Errorf("expected B to be valid")
	}

	if testRegex.validate("C") {
		t.Errorf("expected C to be invalid")
	}

	if testRegex.validate("") {
		t.Errorf("expected empty string to be invalid")
	}
}
*/
